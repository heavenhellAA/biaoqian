<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <title>北京迪赛奇正科技客户标签打印</title>
    <link rel="stylesheet" href="../../Scripts_UI/layui/css/layui.css" media="all">
    <style>
        body {
        }

        .demo-carousel {
            height: 200px;
            line-height: 200px;
            text-align: center;
        }

        .onecode-text {
            text-align: left;
            border: 1px solid #000000;
            /*border-left-style: solid;
            border-right-style: hidden;
            border-bottom-style: hidden;*/
            font-size: 11px;
        }



        .bot {
            border: 0px solid;
            padding: 5px 25px;
            font-size: 15px;
            margin: 5px 20px;
        }

        .font {
            font-weight: bold;
            font-size: 12px;
        }

        .tuoyuan {
            border: 1px solid #000000;
            border-radius: 100%;
            -o-border-radius: 100%;
            -ms-border-radius: 100%;
            -moz-border-radius: 100%;
            -webkit-border-radius: 100%;
        }

    </style>
</head>
<body>
    <div id="layerDemo" style="margin-bottom: 0;">
        <div class="layui-btn-container" style="display:none;">
            <button data-method="notice" class="layui-btn">弹出标签使用方法以及更新说明</button>
        </div>
    </div>
    <!--北京迪赛奇正科技客户标签-->
    <div style="width: 100%; height: 430px; display: none" id="pint_wai">
        <div id="textarea" style="width: 400px; height: 360px; margin-top: 20px; line-height: 25px; margin-left: 50px; text-align: right; display: inline-block;">
            <div style="width: 400px;" class="font">
                厂内标签一维码:
				<input id="cnbqywm" type="text" onblur="Check();" />
            </div>
            <div class="font" style="width: 400px;">
                包装类型:
				<select id="packageType" disabled>
                    <option value="0">示例一</option>
                    <option value="1">示例二(粘贴空间不足)</option>
                </select>
            </div>
            <div style="width: 400px;" class="font">
                物料代码:
				<input id="wldm" type="text" minlength="12" maxlength="16" onblur="getlh();"/>
            </div>
            <div style="width: 400px;" class="font">
                数量:
				<input id="sl" type="text" />
            </div>
            <div style="width: 400px;" class="font">
                生产批次:
				<input id="scpc" type="text"/>
            </div>
            <div style="width: 400px;" class="font">
                生产周期:
				<input id="sczq" type="text" maxlength="4" minlength="4"/>
            </div>
@*            <div style="width: 400px; position: relative; display: normal" class="font">
                <span>生产周期:</span>
                <span>
                    <select id="sczq" onchange="document.getElementById('zq_inp').value = this.value;Getrq();" style="width: 200px;">
                        <option style="display: none;"></option>
                        <option value=""></option>
                    </select>
                </span>
                <input type="text" id="zq_inp" name="zq_inp" style="position: absolute; border: none; width: 180px; left: 202px; top: 4px;" value="" placeholder="请输入进行模糊查询..." onblur="mohusearch();">
            </div>*@
            <div style="width: 400px;" class="font">
                规格型号:
				<input id="ggxx" type="text" />
            </div>
            <div style="width: 400px;" class="font">
                供应商:
				<input id="gys" type="text" value="AIHUA"/>
            </div>
            <div style="width: 400px;" class="font">
                订单:
				<input id="dd" type="text" />
            </div>
            <div style="width: 400px;" class="font">
                品牌:
				<input id="pp" type="text" value="AISHI"/>
            </div>
            <div style="width: 400px;" class="font">
                生产工厂:
				<input id="scgc" type="text" value="CHINA YIYANG"/>
            </div>
            <div style="width: 400px;" class="font">
                唯一码:
				<input id="lh" type="text" style="width:70px;" maxlength="7"/>
                <input id="ip" type="text" style="width:30px;" maxlength="3" value="001" readonly/>
                <input id="gysbm" type="text" style="width:60px;" maxlength="6" value="900854" readonly/>
                <input id="scrq" type="text" style="width:60px;" maxlength="6" readonly/>
                <input id="lsh" type="text" style="width:50px;" maxlength="5" readonly/>
            </div>
            <div style="width: 400px;" class="font">
                打印张数:
				<input type="number" id="numcount" oninput="if(value > 999 || value < 1 ){alert('打印张数范围在1到999之间！');value = ''}" />
            </div>
            <div style="width: 400px;" class="font">
                是否为遗漏补打:
					<label>
                        <input type="checkbox" name="ylbd" value="1">是</label>
            </div>
            <div style="width: 400px; height: 45px; text-align: right;">
                @* <button id="Eliminate" class="bot" style="border: 5px solid #ffd68c; display: inline-block; background-color: orange; color: white;">清空</button>*@
                <button id="Printing" class="bot" style="border: 5px solid #ffd68c; display: inline-block; background-color: seagreen; color: white;">打印</button>
            </div>
        </div>




        <!--右侧打印区域-->
        <!--高度:37*10,长度:270,270-->
        <div id="PrintArea" style="width: 100mm; height: 68mm; vertical-align: top; display: inline-block; margin-left: 20px; white-space: nowrap; font-family: SimSun,monospace;">
            <!--使用表格显示-->
            <table style="border: thin; width: 98mm; height: 66mm; margin-left: 4px; margin-top: 4px;" rules="rows">
                <tbody>

                <tr style="height: 25px">
                    <td colspan="3" id="wldm_msg" class="onecode-text" style="width:250px;border-bottom-style:hidden;font-size: 12px;"><span style="font-size: 11px;">物料代码(CPN):</span></td>
                    <td colspan="1" rowspan="4" style="font-weight: bolder; border: 1px solid #000000; border-left-style:hidden;border-bottom-style:hidden;text-align: center; width: 100px;">
                        <div style="width: 95px; margin: 0 auto" id="qrcode" class="qrcode"></div>
                     </td>
                </tr>
                <tr style="height: 25px">
                    <td colspan="3" id="sl_msg" class="onecode-text" style="width:250px;border-bottom-style:hidden;"><span>数量(QTY):</span></td>
                </tr>
                <tr style="height: 25px">
                    <td colspan="3" id="scpc_msg" class="onecode-text" style="width:250px;border-bottom-style:hidden;"><span>生产批次(LOT NO):</span></td>
                </tr>
                <tr style="height: 25px">
                    <td colspan="3" id="sczq_msg" class="onecode-text" style="width:250px;border-bottom-style:hidden;"><span>生产周期(D/C):</span></td>
                </tr>
                <tr style="height: 25px">
                    <td colspan="4" id="ggxx_msg" class="onecode-text" style="width:370px;border-bottom-style:hidden;"><span>规格型号(MPN):</span></td>
                </tr>
                <tr style="height: 25px">
                    <td colspan="4" id="gys_msg" class="onecode-text" style="width:370px;border-bottom-style:hidden;"><span>供应商(SUPPLIER):</span></td>
                </tr>
                <tr style="height: 25px">
                    <td colspan="4" id="dd_msg" class="onecode-text" style="width:370px;border-bottom-style:hidden;"><span>订单(PO):</span></td>
                </tr>
                <tr style="height: 25px">
                    <td colspan="4" id="pp_msg" class="onecode-text" style="width:370px;border-bottom-style:hidden;"><span>品牌(BRAND):</span></td>
                </tr>
                <tr style="height: 25px">
                    <td colspan="4" id="scgc_msg" class="onecode-text" style="width:370px;border-bottom-style:hidden;"><span>生产工厂(MANUFACTURER):</span></td>
                </tr>
                <tr style="height: 25px">
                    <td colspan="4" id="wym_msg" class="onecode-text" style="width:370px;"><span>唯一码(SN):</span></td>
                </tr>
                </tbody>
            </table>

        </div>

    </div>

    <!--北京迪赛奇正科技客户标签--示例二标签(空间不足粘贴)-->
    <div style="width: 100%; height: 430px; display: none" id="pint_zx">
        <div id="zx_textarea" style="width: 400px; height: 360px; margin-top: 20px; line-height: 25px; margin-left: 50px; text-align: right; display: inline-block;">
            <div style="width: 400px;" class="font">
                厂内标签一维码:
				<input id="zx_cnbqywm" type="text" onblur="Checkzx();" />
            </div>
            <div class="font" style="width: 400px;">
                包装类型:
				<select id="zx_packageType" disabled>
                    <option value="1">示例二(粘贴空间不足)</option>
                    <option value="0">示例一</option>
                </select>
            </div>
            <div style="width: 400px;" class="font">
                物料代码:
				<input id="zx_wldm" type="text" minlength="12" maxlength="16" onblur="getlhzx();"/>
            </div>
            <div style="width: 400px;" class="font">
                数量:
				<input id="zx_sl" type="text" />
            </div>
            <div style="width: 400px;" class="font">
                生产批次:
				<input id="zx_scpc" type="text"/>
            </div>
            <div style="width: 400px;" class="font">
                生产周期:
				<input id="zx_sczq" type="text" maxlength="4" minlength="4"/>
            </div>
            <div style="width: 400px;" class="font">
                规格型号:
				<input id="zx_ggxx" type="text" />
            </div>
            <div style="width: 400px;" class="font">
                供应商:
				<input id="zx_gys" type="text" value="AIHUA"/>
            </div>
            <div style="width: 400px;" class="font">
                订单:
				<input id="zx_dd" type="text" />
            </div>
            <div style="width: 400px;" class="font">
                品牌:
				<input id="zx_pp" type="text" value="AISHI"/>
            </div>
            <div style="width: 400px;" class="font">
                生产工厂:
				<input id="zx_scgc" type="text" value="CHINA YIYANG"/>
            </div>
            <div style="width: 400px;" class="font">
                唯一码:
				<input id="zx_lh" type="text" style="width:70px;" maxlength="7"/>
                <input id="zx_ip" type="text" style="width:30px;" maxlength="3" value="001" readonly/>
                <input id="zx_gysbm" type="text" style="width:60px;" maxlength="6" value="900854" readonly/>
                <input id="zx_scrq" type="text" style="width:60px;" maxlength="6" readonly/>
                <input id="zx_lsh" type="text" style="width:50px;" maxlength="5" readonly/>
            </div>
            <div style="width: 400px;" class="font">
                打印张数:
				<input type="number" id="zx_numcount" oninput="if(value > 999 || value < 1 ){alert('打印张数范围在1到999之间！');value = ''}" />
            </div>
            <div style="width: 400px;" class="font">
                是否为遗漏补打:
					<label>
                        <input type="checkbox" name="zx_ylbd" value="1">是</label>
            </div>
            <div style="width: 400px; height: 45px; text-align: right;">
                @* <button id="Eliminate" class="bot" style="border: 5px solid #ffd68c; display: inline-block; background-color: orange; color: white;">清空</button>*@
                <button id="zx_Printing" class="bot" style="border: 5px solid #ffd68c; display: inline-block; background-color: seagreen; color: white;">打印</button>
            </div>
        </div>




        <!--右侧打印区域-->
        <!--高度:17*6,长度:-->
        <div id="zx_PrintArea" style="width: 80mm; height: 30mm; vertical-align: top; display: inline-block; margin-left: 20px; white-space: nowrap; font-family: SimSun,monospace;">
            <!--使用表格显示-->
            <table style="border: thin; width: 78mm; height: 28mm; margin-left: 4px; margin-top: 4px;" rules="rows">
                <tbody>

                <tr style="height: 17px">
                    <td colspan="1" rowspan="5" style="font-weight: bolder; border: 1px solid #000000; border-right-style:hidden;border-bottom-style:hidden;text-align: center; width: 90px;">
                        <div style="width: 80px; margin: 0 auto" id="zx_qrcode" class="qrcode"></div>
                     </td>
                    <td colspan="3" id="zx_lh_msg" class="onecode-text" style="border-bottom-style:hidden;font-size:12px;"><span style="font-size:11px;">料号:</span></td>

                </tr>
                <tr style="height: 17px">
                    <td colspan="3" id="zx_sl_msg" class="onecode-text" style="border-bottom-style:hidden;"><span>数量:</span></td>
                </tr>
                <tr style="height: 17px">
                    <td colspan="3" id="zx_scpc_msg" class="onecode-text" style="border-bottom-style:hidden;"><span>批号:</span></td>
                </tr>
                <tr style="height: 17px">
                    <td colspan="3" id="zx_sczq_msg" class="onecode-text" style="border-bottom-style:hidden;"><span>周期:</span></td>
                </tr>
                <tr style="height: 17px">
                    <td colspan="3" id="zx_dd_msg" class="onecode-text" style="border-bottom-style:hidden;"><span>订单:</span></td>
                </tr>
                <tr style="height: 17px">
                    <td colspan="4" id="zx_ls_msg" class="onecode-text" style="padding-left:5px;"><span>流水:</span></td>
                </tr>
                </tbody>
            </table>

        </div>

    </div>



    <!-- layui 打印记录表-->
    <table id="demo" lay-filter="test"></table>

    <div class="layui-tab layui-tab-brief" lay-filter="demo">
        <div class="layui-tab-content">
            <div class="layui-tab-item layui-show">

                <div class="layui-carousel" id="test1">
                    <div carousel-item>
                        <div>
                            <p class="layui-bg-green demo-carousel">本后台系统UI为layui！</p>
                        </div>
                        <div>
                            <p class="layui-bg-red demo-carousel">本系统不适用谷歌浏览器请勿用谷歌浏览器进行标签打印</p>
                        </div>
                        <div>
                            <p class="layui-bg-blue demo-carousel">有任何使用上的问题即时反馈</p>
                        </div>
                        <div>
                            <p class="layui-bg-orange demo-carousel">后续功能正在增加完善中</p>
                        </div>
                        <div>
                            <p class="layui-bg-cyan demo-carousel">多沟通！多沟通！多沟通！重要的事情说三遍</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>


    </div>
    <script type="text/html" id="toolbarGroup">
        <div class="layui-btn-container">
            <button class="layui-btn layui-btn-sm layui-icon" lay-event="zfei">&#x1007;作废</button>
            <button class="layui-btn layui-btn-sm layui-icon" lay-event="print_wai">&#xe66d;北京迪赛奇正科技标签打印(示例一)</button>
            <button class="layui-btn layui-btn-sm layui-icon" lay-event="print_zx">&#xe66d;北京迪赛奇正科技标签打印(示例二)</button>
            <button class="layui-btn layui-btn-sm layui-icon" lay-event="print_bd">&#xe66d;破损补打</button>
            <button class="layui-btn layui-btn-sm layui-icon" lay-event="flush">&#xe669;刷新</button>
            <button class="layui-btn layui-btn-sm layui-icon" lay-event="readhowtouse">&#xe702;帮助说明</button>
        </div>
    </script>

    <script src="../../Scripts_UI/layui/layui.all.js" charset="utf-8"></script>
    <script src="../../Scripts/jquery-2.1.1.min.js" charset="utf-8"></script>
    <script src="../../Scripts/jquery-barcode-2.0.2.min.js" charset="utf-8"></script>
    <script src="../../Scripts/barcode.js" charset="utf-8"></script>
    <script src="../../Scripts/xybself.js?version=1.1" charset="utf-8"></script>
                
    <script>
        layui.config({
            version: '1594038786020' //为了更新 js 缓存，可忽略
        });
        var tsdiv = '<div style="padding: 10px; line-height: 21px; background-color: #C8C8C8; color: #000; font-weight: 490;">'
        tsdiv +='<h1>标签打印通用使用方法：</h1>'
        tsdiv +='<ul style="font-size: 18px;">'
        tsdiv +='<li>1:鼠标光标移至厂内标签一维码后输入框内,通过USB扫描枪扫描厂内合格证一维码</li>'
        tsdiv +='<li>2:扫描出一维码信息后鼠标移出输入框任意区域点击即可</li>'
        tsdiv +='<li>3:系统自动查询出相关信息后，空白没有信息的输入框需要手动输入</li>'
        tsdiv +='<li>4:遗漏补打,会自动新增一条标记为补打的新记录,不适用于不能断码标签.</li>'
        tsdiv +='<li>5:破损补打需要选中一条记录,补打完后，该记录会标记为补打状态.</li>'
        tsdiv +='<li>6:打印失败或者试验打印的记录小批量请自行作废处理</li>'
        tsdiv +='<li>7:当客户标签存在流水号,若周期和流水号存在于同一字段里,新周期开始使用不会重码</li>'
        tsdiv +='<li>8:当客户标签存在流水号,且除流水号外无其他变量时，对接使用请联系IT.</li>'
        tsdiv +='<li>9:请输入进行模糊查询字样，可以输入N位数字模糊查询周期,点击下拉框看到查询结果</li>'
        tsdiv +='</ul>'
        tsdiv +='<h1><font color="red">本客户标签特色功能说明：</font></h1>'
        tsdiv +='<ul style="font-size: 18px;">'
        tsdiv +='<li>1:示例一标签大小为100*100，示例二标签大小为80*30</li>'
        tsdiv +='<li>2:唯一码组成为:料号前7码(不含非字母数字的字符)+电脑识别码(IP后三码)+供应商编码(6码)+年月日(6码当前打印日期)+流水号(5码)</li>'
        tsdiv += '<li>3:流水号按照当前打印日期从0开始递增</li>'
        tsdiv +='<li>4:物料代码请手动输入(存在新旧料号区分),最小值为12位旧物料代码,最大值位16位</li>'
        tsdiv +='<li>5:电脑识别码固定为001</li>'
        tsdiv += '<li>6:供应商编码取得C200900854后6位.</li>'
        tsdiv += '</ul>'
        tsdiv += '</div>'
        if (CookieUtil.get("gbk") == 1) {
        } else {
            addhowtouse(tsdiv);
        }      
        function Check() {
            var cnbqywm = document.getElementById("cnbqywm").value;
            $.ajax({
                type: "GET",
                url: '../Datainself/Getdata',
                data: { scph: cnbqywm },
                dataType: "json",
                async: false,  //设置异步，必须先更改对应值才能做后续的传递参数操作
                success: function (data) {
                    //console.log(data.Tables[0].Rows[0]);
                    document.getElementById("dd").value = data.Tables[0].Rows[0].customer_order_id;
                    //document.getElementById("wldm").value = data.Tables[0].Rows[0].customer_goods_id;
                    document.getElementById("scpc").value = data.Tables[0].Rows[0].number;
                    if (data.Tables[0].Rows[0].dc.substring(0, 1) == 'L') {
                        var date = new Date();
                        var str = date.format("yyyy-MM-dd");
                        var zqdm = str.substring(2, 4);
                        document.getElementById("sczq").value = zqdm + data.Tables[0].Rows[0].dc.substring(2, 4);
                    } else {
                        document.getElementById("sczq").value = data.Tables[0].Rows[0].dc;
                    }
                    //document.getElementById("sczq").value = data.Tables[0].Rows[0].dc;
                    document.getElementById("ggxx").value = data.Tables[0].Rows[0].goods_name + ' ' + data.Tables[0].Rows[0].size_id + ' ' + data.Tables[0].Rows[0].terminal;
                    var sjtime = new Date();
                    document.getElementById("scrq").value = sjtime.format("yyyy-MM-dd").replace('-', '').replace('-', '').substring(2, 8);
                    lsnum(sjtime.format("yyyy-MM-dd").replace('-', '').replace('-', '').substring(2, 8));
                    document.getElementById("sl").value = data.Tables[0].Rows[0].qty;

                },
                error: function (res) {
                    console.log(res);
                    layer.msg('查询出错或者无数据！', { icon: 0 });
                }
            });
        }
        function Checkzx() {
            var cnbqywm = document.getElementById("zx_cnbqywm").value;
            $.ajax({
                type: "GET",
                url: '../Datainself/Getdata',
                data: { scph: cnbqywm },
                dataType: "json",
                async: false,  //设置异步，必须先更改对应值才能做后续的传递参数操作
                success: function (data) {
                    //console.log(data.Tables[0].Rows[0]);
                    document.getElementById("zx_dd").value = data.Tables[0].Rows[0].customer_order_id;
                    //document.getElementById("wldm").value = data.Tables[0].Rows[0].customer_goods_id;
                    document.getElementById("zx_scpc").value = data.Tables[0].Rows[0].number;
                    if (data.Tables[0].Rows[0].dc.substring(0, 1) == 'L') {
                        var date = new Date();
                        var str = date.format("yyyy-MM-dd");
                        var zqdm = str.substring(2, 4);
                        document.getElementById("zx_sczq").value = zqdm + data.Tables[0].Rows[0].dc.substring(2, 4);
                    } else {
                        document.getElementById("zx_sczq").value = data.Tables[0].Rows[0].dc;
                    }
                    //document.getElementById("zx_sczq").value = data.Tables[0].Rows[0].dc;
                    document.getElementById("zx_ggxx").value = data.Tables[0].Rows[0].goods_name + ' ' + data.Tables[0].Rows[0].size_id + ' ' + data.Tables[0].Rows[0].terminal;
                    var sjtime = new Date();
                    document.getElementById("zx_scrq").value = sjtime.format("yyyy-MM-dd").replace('-', '').replace('-', '').substring(2, 8);
                    lsnumzx(sjtime.format("yyyy-MM-dd").replace('-', '').replace('-', '').substring(2, 8));
                    document.getElementById("zx_sl").value = data.Tables[0].Rows[0].qty;

                },
                error: function (res) {
                    console.log(res);
                    layer.msg('查询出错或者无数据！', { icon: 0 });
                }
            });
        }
        function getlh() {
            document.getElementById("lh").value = document.getElementById("wldm").value.substring(0, 7);
        }
        function getlhzx() {
            document.getElementById("zx_lh").value = document.getElementById("zx_wldm").value.substring(0, 7);
        }
        function lsnum(scrq) {
            $.ajax({
                type: "GET",
                url: '../BjDisai/Getlsnum',
                data: { scrq: scrq },
                dataType: "text",
                success: function (data) {
                    $("#lsh").val(data);
                },
                error: function (res) {
                    console.log(res);
                }
            });
        }
        function lsnumzx(scrq) {
            $.ajax({
                type: "GET",
                url: '../BjDisai/Getlsnum',
                data: { scrq: scrq },
                dataType: "text",
                success: function (data) {
                    $("#zx_lsh").val(data);
                },
                error: function (res) {
                    console.log(res);
                }
            });
        }
        ////获取周期
        //function zq(zqm) {
        //    $.ajax({
        //        type: "GET",
        //        url: '../Datainself/GetZqdata',
        //        data: { zqm: zqm },
        //        dataType: "json",
        //        async: false,  //设置异步，必须先更改对应值才能做后续的传递参数操作
        //        success: function (data) {
        //            $("#sczq").empty();
        //            $("#sczq").append("<option style='display: none;'></option><option value=''></option>");
        //            for (var i = 0; i < data.Tables[0].Rows.length; i++) {
        //                $("#sczq").append("<option value=" + data.Tables[0].Rows[i].s_zqm + ">" + data.Tables[0].Rows[i].s_zqm + "</option>")
        //            }
        //        },
        //        error: function (res) {
        //            console.log(res);
        //        }
        //    });
        //}
        //function mohusearch() {
        //    var zqm = document.getElementById("zq_inp").value;
        //    zq(zqm);
        //}
        //function Getrq() {
        //    var zqm = document.getElementById("zq_inp").value;
        //    $.ajax({
        //        type: "GET",
        //        url: '../Datainself/GetRqdata',
        //        data: { zqm: zqm },
        //        dataType: "json",
        //        async: false,  //设置异步，必须先更改对应值才能做后续的传递参数操作
        //        success: function (data) {
        //            document.getElementById("scrq").value = data.Tables[0].Rows[0].s_scrq.substring(2, 8);
        //            lsnum(data.Tables[0].Rows[0].s_scrq.substring(2, 8));
        //        },
        //        error: function (res) {
        //            console.log(res);
        //        }
        //    });
        //}

        function showmsg() {
            var wldm = document.getElementById("wldm").value;//物料代码
            var sl = document.getElementById("sl").value;//数量
            var scpc = document.getElementById("scpc").value;//生产批次
            var sczq = document.getElementById("sczq").value;//生产周期
            var ggxx = document.getElementById("ggxx").value;//规格型号
            var gys = document.getElementById("gys").value;//供应商
            var dd = document.getElementById("dd").value;//订单号
            var pp = document.getElementById("pp").value;//品牌
            var scgc = document.getElementById("scgc").value;//生产工厂
            //var lh = document.getElementById("lh").value;//料号前7码
            //var gysbm = document.getElementById("gysbm").value;//供应商编码
            //var scrq = document.getElementById("scrq").value;//生产日期
            //var ip = document.getElementById("ip").value;//IP码
            //var lsh = document.getElementById("lsh").value;//流水号

            $("#wldm_msg").html('<span style="font-size: 11px;">物料代码(CPN):</span>' + wldm);
            $("#sl_msg").html('<span>数量(QTY):</span>' + sl);
            $("#scpc_msg").html('<span>生产批次(LOT NO):</span>' + scpc);
            $("#sczq_msg").html('<span>生产周期(D/C):</span>' + sczq);
            $("#ggxx_msg").html('<span>规格型号(MPN):</span>' + ggxx);
            $("#gys_msg").html('<span>供应商(SUPPLIER):</span>' + gys);
            $("#dd_msg").html('<span>订单(PO):</span>' + dd);
            $("#pp_msg").html('<span>品牌(BRAND):</span>' + pp);
            $("#scgc_msg").html('<span>生产工厂(MANUFACTURER):</span>' + scgc);
           
        }

        function showmsgzx() {
            var wldm = document.getElementById("zx_wldm").value;//物料代码
            var sl = document.getElementById("zx_sl").value;//数量
            var scpc = document.getElementById("zx_scpc").value;//生产批次
            var sczq = document.getElementById("zx_sczq").value;//生产周期
            //var ggxx = document.getElementById("zx_ggxx").value;//规格型号
            //var gys = document.getElementById("zx_gys").value;//供应商
            var dd = document.getElementById("zx_dd").value;//订单号
            //var pp = document.getElementById("zx_pp").value;//品牌
            //var scgc = document.getElementById("zx_scgc").value;//生产工厂
            //var lh = document.getElementById("zx_lh").value;//料号前7码
            //var gysbm = document.getElementById("zx_gysbm").value;//供应商编码
            //var scrq = document.getElementById("zx_scrq").value;//生产日期
            //var ip = document.getElementById("zx_ip").value;//IP码
            //var lsh = document.getElementById("zx_lsh").value;//流水号

            $("#zx_lh_msg").html('<span style="font-size: 11px;">料号:</span>' + wldm);
            $("#zx_sl_msg").html('<span>数量:</span>' + sl);
            $("#zx_scpc_msg").html('<span>批号:</span>' + scpc);
            $("#zx_sczq_msg").html('<span>周期:</span>' + sczq);
            $("#zx_dd_msg").html('<span>订单:</span>' + dd);

        }

        layui.use(['laypage', 'layer', 'table', 'carousel', 'upload', 'element'], function () {
            var laydate =
            laypage = layui.laypage //分页
            , layer = layui.layer //弹层
            , table = layui.table //表格
            , carousel = layui.carousel //轮播
            , element = layui.element //元素操作
            , form = layui.form //表单


            //向世界问个好
            layer.msg('欢迎使用艾华集团标签打印系统');

            //监听Tab切换
            element.on('tab(demo)', function (data) {
                layer.tips('切换了 ' + data.index + '：' + this.innerHTML, this, {
                    tips: 1
                });
            });
            //执行一个 table 实例
            var renderTable = function () {
                table.render({
                    elem: '#demo'
                  , url: '../BjDisai/searchprint'
                    //, where: { n_state: document.getElementById("demoReload").value }
                  , filter: true
                  , sort: true
                , colFilterRecord: true
                , toolbar: '#toolbarGroup' //指向自定义工具栏模板选择器
                  , title: '北京迪赛奇正标签打印记录表'
                      , response: {
                          statusName: 'code' //规定数据状态的字段名称，默认：code
                        , statusCode: 200 //规定成功的状态码，默认：0
                      }
                      , parseData: function (res) { //res 即为原始返回的数据
                          return {
                              "code": res.Tables[0].Code, //解析接口状态s
                              "data": res.Tables[0].Rows, //解析数据列表
                              "msg": res.Tables[0].Name, //解析提示文本
                              "count": res.Tables[1].Rows[0].zongshu, //解析数据长度
                          };
                      }
                      , cols: [[ //表头
                          { type: 'checkbox', fixed: true }
                        , { field: 'n_id', title: '序号', width: 80, fixed: true }
                        , { field: 's_id', title: '唯一标识', width: 110, hide: true }
                        , { field: 's_cnbqywm', title: '厂内标签一维码', width: 170 }
                        , { field: 's_packageType', title: '标签类型', width: 100, templet: function (res) { if (res.s_packageType == '0') return '示例一标签'; else if (res.s_packageType == '1') return '示例二标签'; } }
                        , { field: 's_wldm', title: '物料代码', width: 130 }
                        , { field: 's_sl', title: '数量', width: 90 }
                        , { field: 's_scpc', title: '生产批次', width: 120 }
                        , { field: 's_sczq', title: '生产周期', width: 70 }
                        , { field: 's_ggxx', title: '规格型号', width: 150 }
                        , { field: 's_gys', title: '供应商', width: 100,hide:true }
                        , { field: 's_dd', title: '订单', width: 130 }
                        , { field: 's_pp', title: '品牌', width: 100, hide: true }
                        , { field: 's_scgc', title: '生产工厂', width: 100, hide: true }
                        , { field: 's_wym', title: '唯一码', width: 100 }
                        , { field: 's_gysbm', title: '供应商编码', width: 100 }
                        , { field: 's_scrq', title: '生产日期', width: 100 }
                        , { field: 's_lsh', title: '流水号', width: 100 }
                        , { field: 's_codemsg', title: '二维码信息', width: 200 }
                        , { field: 's_waternum', title: '流水号_V1', width: 100 }
                        , { field: 's_UserName', title: '创建人', width: 120 }
                        , { field: 's_createtime', title: '创建时间', width: 160 }
                        , { field: 's_GroupName', title: '分厂', width: 140 }
                        , { field: 'n_bdprint', title: '补打标记', width: 120 }
                        , { field: 's_RoleName', title: '权限级别', width: 160, hide: true }
                      ]]
                    , n_id: 'testReload'
                  , page: true
                  , limits: [10, 50, 100, 500, 1000, 5000, 10000]
                });
            }
            renderTable();
            //执行一个轮播实例
            carousel.render({
                elem: '#test1'
              , width: '100%' //设置容器宽度
              , height: 200
              , arrow: 'none' //不显示箭头
              , anim: 'fade' //切换动画方式
            });

            //头工具栏事件
            table.on('toolbar(test)', function (obj) {
                var checkStatus = table.checkStatus(obj.config.id);
                switch (obj.event) {
                    case 'zfei':
                        if (checkStatus.data.length > 0) {
                            Zuofei(checkStatus);
                        } else {
                            layer.msg('请选择需要作废的打印记录', { icon: 0 });
                        }
                        break;
                    case 'print_wai':
                        document.getElementById("pint_wai").setAttribute("style", "display:block;");
                        break;
                    case 'print_zx':
                        document.getElementById("pint_zx").setAttribute("style", "display:block;");
                        break;
                    case 'print_bd':
                        //document.getElementById("Printing").setAttribute("style", "display:none;");
                        //document.getElementById("n_Printing").setAttribute("style", "display:none;");
                        if (checkStatus.data.length > 0 && checkStatus.data.length == 1) {
                            Bdprint(checkStatus);
                        }
                        else {
                            layer.msg('请选中需要补打的条码记录，补打功能只支持单条打印！', { icon: 0 });
                        }
                        break;
                    case 'flush':
                        renderTable();
                        break;
                    case 'readhowtouse':
                        addhowtouse(tsdiv);
                        break;
                };
            });

            //北京迪赛奇正客户标签打印事件
            $("#Printing").click(function () {
                var str = document.getElementsByName('ylbd');
                var ylbd = 0;
                if (str[0].checked == true) {
                    ylbd = str[0].value;
                }
                var x = document.getElementById("textarea").getElementsByTagName("input");
                //填写信息是否完整
                for (var i = 0; i < x.length; i++) {
                    if (x[i].value == "") {
                        alert('信息未填写完整');
                        x[i].focus();
                        return false;// 有空值
                    }
                }
                var cnbqywm = document.getElementById("cnbqywm").value;//厂内标签一维码
                var packageType = document.getElementById("packageType").value;//厂内标签一维码              
                var wldm = document.getElementById("wldm").value;//物料代码
                var sl = document.getElementById("sl").value;//数量
                var scpc = document.getElementById("scpc").value;//生产批次
                var sczq = document.getElementById("sczq").value;//生产周期
                var ggxx = document.getElementById("ggxx").value;//规格型号
                var gys = document.getElementById("gys").value;//供应商
                var dd = document.getElementById("dd").value;//订单号
                var pp = document.getElementById("pp").value;//品牌
                var scgc = document.getElementById("scgc").value;//生产工厂
                var lh = document.getElementById("lh").value;//料号前7码
                var gysbm = document.getElementById("gysbm").value;//供应商编码
                var scrq = document.getElementById("scrq").value;//生产日期
                var ip = document.getElementById("ip").value;//IP码
                var num_print = document.getElementById("numcount").value;//打印张数
                if (!confirm("您确定要打印吗?")) return;
                showmsg();
                //添加打印数据数据
                $.ajax({
                    type: "POST",
                    url: "/BjDisai/AddBjDisaiPrint",
                    data: {
                        cnbqywm: cnbqywm,
                        packageType:packageType,
                        wldm: wldm,
                        sl: sl,
                        scpc: scpc,
                        sczq: sczq,
                        ggxx: ggxx,
                        gys: gys,
                        dd: dd,
                        pp: pp,
                        scgc: scgc,
                        lh: lh,
                        gysbm: gysbm,
                        scrq:scrq,
                        ip: ip,
                        num_print: num_print,
                        ylbd: ylbd
                    },
                    dataType: "text",
                    success: function (data) {
                        var str = data.substring(3);
                        var arr = str.split(',');
                        if (data.substring(0, 1) > 0) {
                            var num1 = $("#numcount").val();
                            var headstr = "<html><body>";
                            var footstr = "</body></html>";
                            var printData1 = '';
                            var fenye = '<div style="page-break-after:always;"></div>';    //分页


                            for (i = 0 ; i < num1 ; i++) {
                                lsh = arr[i];//流水号
                                $("#wym_msg").html('<span>唯一码(SN):</span>' + lh + ip + gysbm + scrq + lsh);
                                $('#qrcode').html('');
                                //二维码规则
                                QRCodes = wldm + '|' + sl + '|' + scpc + '|' + sczq + '|' + ggxx + '|' + gys + '|' + dd + '|' + pp + '|' + scgc + '|' + lh + ip + gysbm + scrq + lsh;
                                qrcode = new QRCode(document.getElementById("qrcode"), {
                                    text: QRCodes,
                                    width: 95, //设置宽高
                                    height: 95,
                                    correctLevel: 1,

                                });
                                //能抓到第一个【0】的canvas转成IMG IMG 但是格式不对

                                var img = $("#qrcode").find("canvas")[0].toDataURL("image/png");
                                $("#qrcode").find("canvas").attr('style', 'display:none');
                                $("#qrcode").find("img").attr('src', img);
                                $("#qrcode").find("img").attr('style', 'display:block');

                                printData1 += document.getElementById("PrintArea").innerHTML;
                                if (i != num1 - 1) {
                                    printData1 = printData1 + fenye;
                                }
                            }

                            var printData = printData1;// 获得 div 里的所有 html 数据
                            document.body.innerHTML = headstr + printData + footstr;

                            // 二维码  一维码 可能是太长了 必须等待 要不然打印出来没有二维码
                            window.setTimeout(function () {
                                location.href = location.href;  //返回主界面
                                window.print();
                            }, 3000);

                        } else if (data.substring(0, 2) == -1) {
                            alert("请不要输入非法字符,如果属于必输内容，请联系IT！");
                        } else {
                            alert("500,打印记录生成失败，本次打印终止。");
                        }
                    },
                    error: function (data) {
                        alert("501,打印记录生成失败，本次打印终止。");
                    }
                })

            })

            //北京迪赛奇正客户标签打印事件-示例二
            $("#zx_Printing").click(function () {
                var str = document.getElementsByName('zx_ylbd');
                var ylbd = 0;
                if (str[0].checked == true) {
                    ylbd = str[0].value;
                }
                var x = document.getElementById("zx_textarea").getElementsByTagName("input");
                //填写信息是否完整
                for (var i = 0; i < x.length; i++) {
                    if (x[i].value == "") {
                        alert('信息未填写完整');
                        x[i].focus();
                        return false;// 有空值
                    }
                }
                var cnbqywm = document.getElementById("zx_cnbqywm").value;//厂内标签一维码
                var packageType = document.getElementById("zx_packageType").value;//厂内标签一维码              
                var wldm = document.getElementById("zx_wldm").value;//物料代码
                var sl = document.getElementById("zx_sl").value;//数量
                var scpc = document.getElementById("zx_scpc").value;//生产批次
                var sczq = document.getElementById("zx_sczq").value;//生产周期
                var ggxx = document.getElementById("zx_ggxx").value;//规格型号
                var gys = document.getElementById("zx_gys").value;//供应商
                var dd = document.getElementById("zx_dd").value;//订单号
                var pp = document.getElementById("zx_pp").value;//品牌
                var scgc = document.getElementById("zx_scgc").value;//生产工厂
                var lh = document.getElementById("zx_lh").value;//料号前7码
                var gysbm = document.getElementById("zx_gysbm").value;//供应商编码
                var scrq = document.getElementById("zx_scrq").value;//生产日期
                var ip = document.getElementById("zx_ip").value;//IP码
                var num_print = document.getElementById("zx_numcount").value;//打印张数
                if (!confirm("您确定要打印吗?")) return;
                showmsgzx();
                //添加打印数据数据
                $.ajax({
                    type: "POST",
                    url: "/BjDisai/AddBjDisaiPrint",
                    data: {
                        cnbqywm: cnbqywm,
                        packageType: packageType,
                        wldm: wldm,
                        sl: sl,
                        scpc: scpc,
                        sczq: sczq,
                        ggxx: ggxx,
                        gys: gys,
                        dd: dd,
                        pp: pp,
                        scgc: scgc,
                        lh: lh,
                        gysbm: gysbm,
                        scrq: scrq,
                        ip: ip,
                        num_print: num_print,
                        ylbd: ylbd
                    },
                    dataType: "text",
                    success: function (data) {
                        var str = data.substring(3);
                        var arr = str.split(',');
                        if (data.substring(0, 1) > 0) {
                            var num1 = $("#zx_numcount").val();
                            var headstr = "<html><body>";
                            var footstr = "</body></html>";
                            var printData1 = '';
                            var fenye = '<div style="page-break-after:always;"></div>';    //分页


                            for (i = 0 ; i < num1 ; i++) {
                                lsh = arr[i];//流水号
                                $("#zx_ls_msg").html('<span>流水:</span>' + lh + ip + gysbm + scrq + lsh);
                                $('#zx_qrcode').html('');
                                //二维码规则
                                QRCodes = wldm + '|' + sl + '|' + scpc + '|' + sczq + '|' + ggxx + '|' + gys + '|' + dd + '|' + pp + '|' + scgc + '|' + lh + ip + gysbm + scrq + lsh;
                                qrcode = new QRCode(document.getElementById("zx_qrcode"), {
                                    text: QRCodes,
                                    width: 80, //设置宽高
                                    height: 80,
                                    correctLevel: 1,

                                });
                                //能抓到第一个【0】的canvas转成IMG IMG 但是格式不对

                                var img = $("#zx_qrcode").find("canvas")[0].toDataURL("image/png");
                                $("#zx_qrcode").find("canvas").attr('style', 'display:none');
                                $("#zx_qrcode").find("img").attr('src', img);
                                $("#zx_qrcode").find("img").attr('style', 'display:block');

                                printData1 += document.getElementById("zx_PrintArea").innerHTML;
                                if (i != num1 - 1) {
                                    printData1 = printData1 + fenye;
                                }
                            }

                            var printData = printData1;// 获得 div 里的所有 html 数据
                            document.body.innerHTML = headstr + printData + footstr;

                            // 二维码  一维码 可能是太长了 必须等待 要不然打印出来没有二维码
                            window.setTimeout(function () {
                                location.href = location.href;  //返回主界面
                                window.print();
                            }, 3000);

                        } else if (data.substring(0, 2) == -1) {
                            alert("请不要输入非法字符,如果属于必输内容，请联系IT！");
                        } else {
                            alert("500,打印记录生成失败，本次打印终止。");
                        }
                    },
                    error: function (data) {
                        alert("501,打印记录生成失败，本次打印终止。");
                    }
                })

            })

            //作废打印记录(多选)
            function Zuofei(obj) {
                var str = "";
                var msg = "";
                var l = obj.data.length;
                for (var i = 0; i < l; i++) {
                    str += "'" + obj.data[i].n_id + "',";
                }
                layer.confirm('确定要作废' + l + '条记录吗?', { icon: 2, title: '提示' }, function (index) {
                    layui.jquery.ajax({
                        type: "POST",
                        url: '../BjDisai/Zuofei',
                        data: { delstr: str },
                        dataType: "JSON",
                        success: function (res) {
                            if (res > 0) {
                                var msg = "成功作废" + res + "条记录！";
                                layer.msg(msg, { icon: 6, time: 1000 }, function () {
                                    layer.close(index);
                                    renderTable();
                                });
                            }
                            else {
                                layer.msg("作废失败", { icon: 5 });
                            }
                        }
                    });
                });
            }

            //补打打印记录(单个)
            function Bdprint(obj) {
                if (obj.data[0].s_packageType == 0) {
                    document.getElementById("pint_wai").setAttribute("style", "display:block;");

                    document.getElementById("cnbqywm").value = obj.data[0].s_cnbqywm;//厂内标签一维码
                    document.getElementById("wldm").value = obj.data[0].s_wldm;//物料代码
                    document.getElementById("sl").value = obj.data[0].s_sl;//数量
                    document.getElementById("scpc").value = obj.data[0].s_scpc;//生产批次
                    document.getElementById("sczq").value = obj.data[0].s_sczq;//生产周期
                    document.getElementById("ggxx").value = obj.data[0].s_ggxx;//规格型号
                    document.getElementById("gys").value = obj.data[0].s_gys;//供应商
                    document.getElementById("dd").value = obj.data[0].s_dd;//订单号
                    document.getElementById("pp").value = obj.data[0].s_pp;//品牌
                    document.getElementById("scgc").value = obj.data[0].s_scgc;//生产工厂
                    document.getElementById("lh").value = obj.data[0].s_lh;//料号前7码
                    document.getElementById("gysbm").value = obj.data[0].s_gysbm;//供应商编码
                    document.getElementById("scrq").value = obj.data[0].s_scrq;//生产日期
                    document.getElementById("ip").value = obj.data[0].s_ip;//IP码
                    document.getElementById("lsh").value = obj.data[0].s_lsh;//流水号
                    document.getElementById("numcount").value = 1;//打印张数

                    showmsg();
                    layer.confirm('确定要补打第' + obj.data[0].n_id + '条记录吗?', { icon: 7, title: '提示' }, function (index) {
                        var cnbqywm = document.getElementById("cnbqywm").value;//厂内标签一维码
                        var wldm = document.getElementById("wldm").value;//物料代码
                        var sl = document.getElementById("sl").value;//数量
                        var scpc = document.getElementById("scpc").value;//生产批次
                        var sczq = document.getElementById("sczq").value;//生产周期
                        var ggxx = document.getElementById("ggxx").value;//规格型号
                        var gys = document.getElementById("gys").value;//供应商
                        var dd = document.getElementById("dd").value;//订单号
                        var pp = document.getElementById("pp").value;//品牌
                        var scgc = document.getElementById("scgc").value;//生产工厂
                        var lh = document.getElementById("lh").value;//料号前7码
                        var gysbm = document.getElementById("gysbm").value;//供应商编码
                        var scrq = document.getElementById("scrq").value;//生产日期
                        var ip = document.getElementById("ip").value;//IP码
                        var lsh = document.getElementById("lsh").value;//流水号
                        var num_print = document.getElementById("numcount").value;//打印张数


                        //更新补打标记
                        $.ajax({
                            type: "POST",
                            url: "/BjDisai/UpdatetbBjDisaiPrint_BD",
                            data: {
                                id: obj.data[0].n_id
                            },

                            success: function (data) {
                                if (data > 0) {
                                    var num1 = $("#numcount").val();
                                    var headstr = "<html><body>";
                                    var footstr = "</body></html>";
                                    var printData1 = '';
                                    var fenye = '<div style="page-break-after:always;"></div>';    //分页


                                    for (i = 0 ; i < num1 ; i++) {
                                        $("#wym_msg").html('<span>唯一码(SN):</span>' + lh + ip + gysbm + scrq + lsh);
                                        $('#qrcode').html('');
                                        //二维码规则
                                        QRCodes = wldm + '|' + sl + '|' + scpc + '|' + sczq + '|' + ggxx + '|' + gys + '|' + dd + '|' + pp + '|' + scgc + '|' + lh + ip + gysbm + scrq + lsh;
                                        qrcode = new QRCode(document.getElementById("qrcode"), {
                                            text: QRCodes,
                                            width: 95, //设置宽高
                                            height: 95,
                                            correctLevel: 1,

                                        });
                                        //能抓到第一个【0】的canvas转成IMG IMG 但是格式不对

                                        var img = $("#qrcode").find("canvas")[0].toDataURL("image/png");
                                        $("#qrcode").find("canvas").attr('style', 'display:none');
                                        $("#qrcode").find("img").attr('src', img);
                                        $("#qrcode").find("img").attr('style', 'display:block');

                                        printData1 += document.getElementById("PrintArea").innerHTML;
                                        if (i != num1 - 1) {
                                            printData1 = printData1 + fenye;
                                        }
                                    }

                                    var printData = printData1;// 获得 div 里的所有 html 数据
                                    document.body.innerHTML = headstr + printData + footstr;

                                    // 二维码  一维码 可能是太长了 必须等待 要不然打印出来没有二维码
                                    window.setTimeout(function () {
                                        location.href = location.href;  //返回主界面
                                        window.print();
                                    }, 3000);

                                } else if (data == -1) {
                                    alert("请不要输入非法字符,如果属于必输内容，请联系IT！");
                                } else {
                                    alert("500,打印记录生成失败，本次打印终止。");
                                }
                            },
                            error: function (data) {
                                alert("501,打印记录生成失败，本次打印终止。");
                            }
                        })

                    })
                } else {
                    document.getElementById("pint_zx").setAttribute("style", "display:block;");

                    document.getElementById("zx_cnbqywm").value = obj.data[0].s_cnbqywm;//厂内标签一维码
                    document.getElementById("zx_wldm").value = obj.data[0].s_wldm;//物料代码
                    document.getElementById("zx_sl").value = obj.data[0].s_sl;//数量
                    document.getElementById("zx_scpc").value = obj.data[0].s_scpc;//生产批次
                    document.getElementById("zx_sczq").value = obj.data[0].s_sczq;//生产周期
                    document.getElementById("zx_ggxx").value = obj.data[0].s_ggxx;//规格型号
                    document.getElementById("zx_gys").value = obj.data[0].s_gys;//供应商
                    document.getElementById("zx_dd").value = obj.data[0].s_dd;//订单号
                    document.getElementById("zx_pp").value = obj.data[0].s_pp;//品牌
                    document.getElementById("zx_scgc").value = obj.data[0].s_scgc;//生产工厂
                    document.getElementById("zx_lh").value = obj.data[0].s_lh;//料号前7码
                    document.getElementById("zx_gysbm").value = obj.data[0].s_gysbm;//供应商编码
                    document.getElementById("zx_scrq").value = obj.data[0].s_scrq;//生产日期
                    document.getElementById("zx_ip").value = obj.data[0].s_ip;//IP码
                    document.getElementById("zx_lsh").value = obj.data[0].s_lsh;//流水号
                    document.getElementById("zx_numcount").value = 1;//打印张数

                    showmsgzx();
                    layer.confirm('确定要补打第' + obj.data[0].n_id + '条记录吗?', { icon: 7, title: '提示' }, function (index) {
                        var cnbqywm = document.getElementById("zx_cnbqywm").value;//厂内标签一维码
                        var wldm = document.getElementById("zx_wldm").value;//物料代码
                        var sl = document.getElementById("zx_sl").value;//数量
                        var scpc = document.getElementById("zx_scpc").value;//生产批次
                        var sczq = document.getElementById("zx_sczq").value;//生产周期
                        var ggxx = document.getElementById("zx_ggxx").value;//规格型号
                        var gys = document.getElementById("zx_gys").value;//供应商
                        var dd = document.getElementById("zx_dd").value;//订单号
                        var pp = document.getElementById("zx_pp").value;//品牌
                        var scgc = document.getElementById("zx_scgc").value;//生产工厂
                        var lh = document.getElementById("zx_lh").value;//料号前7码
                        var gysbm = document.getElementById("zx_gysbm").value;//供应商编码
                        var scrq = document.getElementById("zx_scrq").value;//生产日期
                        var ip = document.getElementById("zx_ip").value;//IP码
                        var lsh = document.getElementById("zx_lsh").value;//流水号
                        var num_print = document.getElementById("zx_numcount").value;//打印张数


                        //更新补打标记
                        $.ajax({
                            type: "POST",
                            url: "/BjDisai/UpdatetbBjDisaiPrint_BD",
                            data: {
                                id: obj.data[0].n_id
                            },

                            success: function (data) {
                                if (data > 0) {
                                    var num1 = $("#zx_numcount").val();
                                    var headstr = "<html><body>";
                                    var footstr = "</body></html>";
                                    var printData1 = '';
                                    var fenye = '<div style="page-break-after:always;"></div>';    //分页


                                    for (i = 0 ; i < num1 ; i++) {
                                        $("#zx_ls_msg").html('<span>流水:</span>' + lh + ip + gysbm + scrq + lsh);
                                        $('#zx_qrcode').html('');
                                        //二维码规则
                                        QRCodes = wldm + '|' + sl + '|' + scpc + '|' + sczq + '|' + ggxx + '|' + gys + '|' + dd + '|' + pp + '|' + scgc + '|' + lh + ip + gysbm + scrq + lsh;
                                        qrcode = new QRCode(document.getElementById("zx_qrcode"), {
                                            text: QRCodes,
                                            width: 80, //设置宽高
                                            height: 80,
                                            correctLevel: 1,

                                        });
                                        //能抓到第一个【0】的canvas转成IMG IMG 但是格式不对

                                        var img = $("#zx_qrcode").find("canvas")[0].toDataURL("image/png");
                                        $("#zx_qrcode").find("canvas").attr('style', 'display:none');
                                        $("#zx_qrcode").find("img").attr('src', img);
                                        $("#zx_qrcode").find("img").attr('style', 'display:block');

                                        printData1 += document.getElementById("zx_PrintArea").innerHTML;
                                        if (i != num1 - 1) {
                                            printData1 = printData1 + fenye;
                                        }
                                    }

                                    var printData = printData1;// 获得 div 里的所有 html 数据
                                    document.body.innerHTML = headstr + printData + footstr;

                                    // 二维码  一维码 可能是太长了 必须等待 要不然打印出来没有二维码
                                    window.setTimeout(function () {
                                        location.href = location.href;  //返回主界面
                                        window.print();
                                    }, 3000);

                                } else if (data == -1) {
                                    alert("请不要输入非法字符,如果属于必输内容，请联系IT！");
                                } else {
                                    alert("500,打印记录生成失败，本次打印终止。");
                                }
                            },
                            error: function (data) {
                                alert("501,打印记录生成失败，本次打印终止。");
                            }
                        })

                    })
                }


            }




        });
    </script>
</body>
</html>
